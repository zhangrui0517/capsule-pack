{
  "compilerOptions": {
		/* 跳过对库文件的检查 */
		"skipLibCheck": true,
		/*  支持默认导入 */
		"allowSyntheticDefaultImports":true,
		/* 构建产物的规范 */
    "module": "ES2022",
    /* 生成声明文件，开启后会自动生成声明文件 */
    "declaration": true,
		"emitDeclarationOnly": true,
    /* 构建输出目录 */
    "outDir": "./lib/typing",
    /* 要构建的目录 */
    "rootDir": "./src",
    /* 是否不生成文件 */
    "noEmit": false,
		/* 模块解析 */
    "moduleResolution":"Node"
  },
  "include": ["./src"],
  /* 不检查的目录 */
  "exclude": ["node_modules","./test", "./lib", "./dist", "./template"]
}
